# Schema tests for Interpretation Comment
---
- config:
    - testset: "Schemas API Interpretation Comment test sets"
- test:
    - group: "/api/schemas/interpretationComment"
    - url: "/api/schemas/interpretationComment.json"
    - auth_username: "admin"
    - auth_password: "district"
    - expected_status: [200]
    - validators:
        - compare: {jsonpath_mini: "klass",  comparator: "eq",  expected: "org.hisp.dhis.interpretation.InterpretationComment"}
        - compare: {jsonpath_mini: "relativeApiEndpoint",  comparator: "eq",  expected: "/interpretationComments"}
        - compare: {jsonpath_mini: "plural",  comparator: "eq",  expected: "interpretationComments"}
        - compare: {jsonpath_mini: "displayName",  comparator: "eq",  expected: "Interpretation Comment"}
        - compare: {jsonpath_mini: "collectionName",  comparator: "eq",  expected: "interpretationComments"}
        - compare: {jsonpath_mini: "name",  comparator: "eq",  expected: "interpretationComment"}
        - compare: {jsonpath_mini: "namespace",  comparator: "eq",  expected: "http://dhis2.org/schema/dxf/2.0"}
        - compare: {jsonpath_mini: "singular",  comparator: "eq",  expected: "interpretationComment"}
        - compare: {jsonpath_mini: "authorities",  comparator: "count_eq",  expected: 0}
        - compare: {names: "properties",  comparator: "equal_items",  expected: "['code','userGroupAccess','access','created','displayName','publicAccess','externalAccess','lastUpdated','name','id','href','text','user']"}
