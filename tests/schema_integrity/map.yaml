# Schema tests for Map
---
- config:
    - testset: "Schemas API Map test sets"
- test:
    - group: "/api/schemas/map"
    - url: "/api/schemas/map.json"
    - auth_username: "admin"
    - auth_password: "district"
    - expected_status: [200]
    - validators:
        - compare: {jsonpath_mini: "klass",  comparator: "eq",  expected: "org.hisp.dhis.mapping.Map"}
        - compare: {jsonpath_mini: "relativeApiEndpoint",  comparator: "eq",  expected: "/maps"}
        - compare: {jsonpath_mini: "plural",  comparator: "eq",  expected: "maps"}
        - compare: {jsonpath_mini: "displayName",  comparator: "eq",  expected: "Map"}
        - compare: {jsonpath_mini: "collectionName",  comparator: "eq",  expected: "maps"}
        - compare: {jsonpath_mini: "name",  comparator: "eq",  expected: "map"}
        - compare: {jsonpath_mini: "namespace",  comparator: "eq",  expected: "http://dhis2.org/schema/dxf/2.0"}
        - compare: {jsonpath_mini: "singular",  comparator: "eq",  expected: "map"}
        - compare: {jsonpath_mini: "authorities",  comparator: "count_eq",  expected: 2}
        - compare: {names: "properties",  comparator: "equal_items",  expected: "['code','userGroupAccess','access','created','displayName','publicAccess','latitude','zoom','externalAccess','lastUpdated','name','mapView','id','href','user','longitude']"}
