# Schema tests for Validation Rule
---
- config:
    - testset: "Schemas API Validation Rule test sets"
- test:
    - group: "/api/schemas/validationRule"
    - url: "/api/schemas/validationRule.json"
    - auth_username: "admin"
    - auth_password: "district"
    - expected_status: [200]
    - validators:
        - compare: {jsonpath_mini: "klass",  comparator: "eq",  expected: "org.hisp.dhis.validation.ValidationRule"}
        - compare: {jsonpath_mini: "relativeApiEndpoint",  comparator: "eq",  expected: "/validationRules"}
        - compare: {jsonpath_mini: "plural",  comparator: "eq",  expected: "validationRules"}
        - compare: {jsonpath_mini: "displayName",  comparator: "eq",  expected: "Validation Rule"}
        - compare: {jsonpath_mini: "collectionName",  comparator: "eq",  expected: "validationRules"}
        - compare: {jsonpath_mini: "name",  comparator: "eq",  expected: "validationRule"}
        - compare: {jsonpath_mini: "namespace",  comparator: "eq",  expected: "http://dhis2.org/schema/dxf/2.0"}
        - compare: {jsonpath_mini: "singular",  comparator: "eq",  expected: "validationRule"}
        - compare: {jsonpath_mini: "authorities",  comparator: "count_eq",  expected: 2}
        - compare: {jsonpath_mini: "properties",  comparator: "count_eq",  expected: 26}
        - compare: {jsonpath_mini: "properties.0.name",  comparator: "eq",  expected: "validationRuleGroup"}
        - compare: {jsonpath_mini: "properties.1.name",  comparator: "eq",  expected: "lowOutliers"}
        - compare: {jsonpath_mini: "properties.2.name",  comparator: "eq",  expected: "code"}
        - compare: {jsonpath_mini: "properties.3.name",  comparator: "eq",  expected: "access"}
        - compare: {jsonpath_mini: "properties.4.name",  comparator: "eq",  expected: "importance"}
        - compare: {jsonpath_mini: "properties.5.name",  comparator: "eq",  expected: "displayName"}
        - compare: {jsonpath_mini: "properties.6.name",  comparator: "eq",  expected: "publicAccess"}
        - compare: {jsonpath_mini: "properties.7.name",  comparator: "eq",  expected: "description"}
        - compare: {jsonpath_mini: "properties.8.name",  comparator: "eq",  expected: "operator"}
        - compare: {jsonpath_mini: "properties.9.name",  comparator: "eq",  expected: "externalAccess"}
        - compare: {jsonpath_mini: "properties.10.name",  comparator: "eq",  expected: "lastUpdated"}
        - compare: {jsonpath_mini: "properties.11.name",  comparator: "eq",  expected: "leftSide"}
        - compare: {jsonpath_mini: "properties.12.name",  comparator: "eq",  expected: "ruleType"}
        - compare: {jsonpath_mini: "properties.13.name",  comparator: "eq",  expected: "id"}
        - compare: {jsonpath_mini: "properties.14.name",  comparator: "eq",  expected: "sequentialSampleCount"}
        - compare: {jsonpath_mini: "properties.15.name",  comparator: "eq",  expected: "href"}
        - compare: {jsonpath_mini: "properties.16.name",  comparator: "eq",  expected: "annualSampleCount"}
        - compare: {jsonpath_mini: "properties.17.name",  comparator: "eq",  expected: "highOutliers"}
        - compare: {jsonpath_mini: "properties.18.name",  comparator: "eq",  expected: "userGroupAccess"}
        - compare: {jsonpath_mini: "properties.19.name",  comparator: "eq",  expected: "created"}
        - compare: {jsonpath_mini: "properties.20.name",  comparator: "eq",  expected: "rightSide"}
        - compare: {jsonpath_mini: "properties.21.name",  comparator: "eq",  expected: "periodType"}
        - compare: {jsonpath_mini: "properties.22.name",  comparator: "eq",  expected: "organisationUnitLevel"}
        - compare: {jsonpath_mini: "properties.23.name",  comparator: "eq",  expected: "instruction"}
        - compare: {jsonpath_mini: "properties.24.name",  comparator: "eq",  expected: "name"}
        - compare: {jsonpath_mini: "properties.25.name",  comparator: "eq",  expected: "user"}
